@model CLINIC.Models.tbl_Parameters

@{
    ViewBag.Title = "Nuevo Parámetro";
    Layout = null;
}

<div class="container">
    <div class="card text-center">
        <div class="card-body">
            @using (Html.BeginForm("NewParameter", "Parameter", FormMethod.Post, new { @class = "form", @id = "frmNewParameter" }))
            {
                @Html.AntiForgeryToken()
                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="Name">Parámetro</label>
                            @Html.TextBoxFor(m => m.Parameter, new { @class = "form-control", @id = "Name", @type = "text", @required = "required" })
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="Description">Valor</label>
                            @Html.TextBoxFor(m => m.ValueOfParameter, new { @class = "form-control", @id = "Description", @type = "text", @required = "required" })
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col">
                        <div class="center-align">
                            <button type="submit" class="btn btn-block btn-outline-success">Crear Parámetro</button>
                        </div>
                    </div>
                </div>
            }
        </div>
        <div class="card-footer text-danger">
            <p id="Message">@Html.ValidationSummary(true, "", new { @class = "text-title" })</p>
        </div>
    </div>
</div>